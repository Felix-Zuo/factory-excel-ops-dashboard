{
  "name": "Factory Excel Ops Dashboard",
  "version": "0.3.0-beta.1",
  "purpose": "Classify spreadsheet exports, normalize fields, compute configurable operations metrics, and export a local dashboard.",
  "entrypoints": [
    {
      "name": "run_pipeline",
      "command": "python -m factory_excel_ops.cli run --input <input_dir> --output <output_dir>",
      "inputs": {
        "input_dir": "Folder containing CSV or XLSX files.",
        "output_dir": "Folder where summary.json and dashboard.html will be written.",
        "file_types": "Optional JSON source signature config.",
        "field_mapping": "Optional JSON field alias config.",
        "metrics": "Optional JSON metric profile config.",
        "min_confidence": "Minimum file classification confidence. Defaults to 0.2.",
        "max_file_mb": "Maximum input file size in MB. Defaults to 25. Use 0 to disable."
      },
      "outputs": {
        "summary_json": "output/summary.json",
        "dashboard_html": "output/dashboard.html"
      }
    },
    {
      "name": "validate_config",
      "command": "python -m factory_excel_ops.cli validate-config --file-types <file_types_json> --field-mapping <field_mapping_json> --metrics <metrics_json>",
      "outputs": {
        "status": "Exit code 0 when config is valid, 2 when errors are found."
      }
    },
    {
      "name": "adapt_tables",
      "command": "python -m factory_excel_ops.cli adapt --input <input_dir> --output <profile_dir>",
      "inputs": {
        "input_dir": "Folder containing unfamiliar CSV or XLSX files.",
        "profile_dir": "Folder where generated configs and adaptation_report.json will be written.",
        "enable_model_assist": "Optional boundary help for low-confidence tables. Disabled by default.",
        "api_key_env": "Environment variable that contains the API key. Defaults to FACTORY_EXCEL_OPS_API_KEY.",
        "model_endpoint": "Optional HTTP model endpoint. Can also be set through FACTORY_EXCEL_OPS_MODEL_ENDPOINT.",
        "model": "Optional model name. Can also be set through FACTORY_EXCEL_OPS_MODEL."
      },
      "outputs": {
        "file_types": "<profile_dir>/file_types.json",
        "field_mapping": "<profile_dir>/field_mapping.json",
        "metrics": "<profile_dir>/metrics.json",
        "adaptation_report": "<profile_dir>/adaptation_report.json"
      }
    },
    {
      "name": "export_integration_spec",
      "command": "python -m factory_excel_ops.cli integration-spec --output <path>",
      "outputs": {
        "integration_interface": "Machine-readable integration contract."
      }
    },
    {
      "name": "build_analysis_context",
      "command": "python -m factory_excel_ops.cli analysis-context --summary <summary_json> --output <path>",
      "inputs": {
        "summary_json": "summary.json generated by the pipeline."
      },
      "outputs": {
        "analysis_context": "Structured context for reporting and workflow review."
      }
    },
    {
      "name": "package_project",
      "command": "python scripts/package_project.py --name <package_name> --output <output_dir>",
      "outputs": {
        "zip_file": "Portable source package without cache, output, or local runtime folders."
      }
    }
  ],
  "supported_file_extensions": [".csv", ".xlsx", ".xlsm"],
  "source_types": [
    "inventory",
    "demand",
    "fulfillment",
    "replenishment",
    "work_output"
  ],
  "standard_fields": [
    "item_code",
    "item_name",
    "available_qty",
    "demand_qty",
    "fulfilled_qty",
    "replenishment_qty",
    "output_qty",
    "account",
    "partner",
    "source_date"
  ],
  "config_files": [
    "config/sample_file_types.json",
    "config/sample_field_mapping.json",
    "config/sample_metrics.json",
    "<profile_dir>/file_types.json",
    "<profile_dir>/field_mapping.json",
    "<profile_dir>/metrics.json"
  ],
  "packaging_notes": [
    "Keep local adapters separate from the core package.",
    "Load local field mappings and metric profiles from config before running.",
    "Use adapt before run when a folder contains unfamiliar table headers.",
    "Store API keys in environment variables; generated profile files never contain keys.",
    "Use sample_data only for sample runs and tests.",
    "Write generated reports to output or another ignored folder.",
    "Use scripts/package_project.py to create a clean handoff package."
  ],
  "integration_targets": [
    "spreadsheet drop folder adapter",
    "business system export adapter",
    "service operations export adapter",
    "operations review package",
    "management reporting workbook"
  ]
}
